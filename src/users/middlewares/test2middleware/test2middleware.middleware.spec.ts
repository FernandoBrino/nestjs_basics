import { Test2middlewareMiddleware } from './test2middleware.middleware';

describe('Test2middlewareMiddleware', () => {
  it('should be defined', () => {
    expect(new Test2middlewareMiddleware()).toBeDefined();
  });
});
